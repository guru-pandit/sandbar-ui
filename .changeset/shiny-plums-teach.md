---
"@panux-ui/tokens": minor
---

Add the three-tier design token system: primitive color scales (gray/accent/danger/success/warning, light + dark, WCAG 2.2 AA-verified solid-step pairings) and non-color primitives (space, radii, shadows, z-index, motion, type), a semantic CSS variable contract (`colorVars`/`staticVars`) generated via vanilla-extract for `light`/`dark`/`contrast` themes under the `.panux-ui-theme` root class, and the `themeClassName` export.
