#!/usr/bin/env node
/**
 * @panux-ui/cli — scaffolding and codemods (`panux init`, `panux add <component>`, `panux theme`).
 *
 * Phase 1 scaffold placeholder. Real commands land in Phase 9 — see
 * .claude/context/architecture.md §Execution Order.
 */
export const version = '0.0.0';
