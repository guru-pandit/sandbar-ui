---
title: Introduction
description: Sandbar UI — the container for your interface.
---

Sandbar UI is a production-grade, accessible, themeable React 19 / Next.js 15 component library.

This docs site is itself built with `@sandbar-ui/react` — the primary dogfooding surface, per `.claude/context/architecture.md`.

## Status

This page is running through the real Fumadocs MDX pipeline (`fumadocs-mdx` + `fumadocs-core` + `fumadocs-ui`, pinned to the Next.js 15–compatible line — see `docs/adr/0001-styling-engine-and-rsc-strategy.md`). The full docs IA (`/docs/components/[slug]`, live Sandpack playgrounds, Orama search, auto-generated props tables) lands incrementally as real components ship, starting Phase 5.
