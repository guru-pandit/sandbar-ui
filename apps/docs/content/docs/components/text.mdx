---
title: Text
description: Inline/block text with token-driven size, weight, and color variants.
---

```tsx
import { Text } from '@sandbar-ui/react';
```

## Usage

<TextUsageExample />

## Examples

### Sizes

Use the `size` prop to change the font size of the text.

<TextSizesExample />

### Weights

Use the `weight` prop to change the font weight.

<TextWeightsExample />

### Colors

Use the `color` prop to change the text color. Every value reads a semantic token, so it automatically follows the active theme (`light`/`dark`/`contrast`).

<TextColorsExample />

## Props

<PropsTable component="Text" />
